@model IEnumerable<KENTAS.Models.Advert>

@{
    ViewBag.Title = "Duyurular";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section css{

    <style type="text/css">
        .hbg {
            background: url(../Content/images/f_bg.jpg) center center no-repeat;
            background-size: cover;
        }
    </style>
}



<div class="container bgwhite mt-4">

    <div class="row p-1">

        <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12 radius_contains p-3" style="background-color:#00a0af;background-image:url(/Content/images/f_bg.jpg)">
            <h6 class="pl-3 my-auto" style="color:white">DUYURULAR</h6>
        </div>

    </div>

    <div style="height:800px; overflow-y:auto; overflow-x:hidden;">

        <div class="row">

            <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12 py-2 px-5">


                @foreach (var item in Model.ToList())
                {
                    <div class="row border radius_contains mb-3 ">
                        <div class="col-xl-3 col-lg-3 col-md-12 col-sm-12 col-12 pt-2 pb-2">
                            <img src="@Url.Content(item.adImage)" class="radius_contains" style="width: 100%;" alt="duyuru" />
                        </div>
                        <div class="col-xl-9 col-lg-9 col-md-12 col-sm-12 col-12 pt-xl-4 pt-lg-4 pt-md-2 ">
                            <h6 class="mb-1 mt-2 text-uppercase" style="color:midnightblue;"> @Html.Raw(item.adHeaderDesc)</h6>
                            <h6 class="mb-1 newsElipsis" style="color:midnightblue;">  @Html.Raw(item.adDesc)</h6>
                            <p class="mb-0 " style="font-size:14px;color: midnightblue; "></p>
                            <a style="font-size:14px; color:#00a0af; " class="mt-1">@Html.ActionLink("Detaylı İncele", "Details", new { id = item.adId })</a>
                        </div>
                    </div>
                }
            </div>



        </div>

    </div>

</div>

@section scripts{
    <script type="text/javascript">
        $($('[href="' + location.pathname + '"]')[0].parentElement).addClass('active');
    </script>
}
@*<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.adHeaderDesc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.adDesc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.adTab)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.adImage)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.Raw(item.adHeaderDesc)
            </td>
            <td>
                @Html.Raw(item.adDesc)
            </td>
            <td>
                @Html.Raw(item.adTab)
            </td>
            <td>
                <img src="@Url.Content(item.adImage)" style="height:100px;" alt="Advert" />
            </td>
            <td>

                @Html.ActionLink("Detaylı İncele", "Details", new { id=item.adId })

            </td>
        </tr>
    }

    </table>*@
